def count(number, mu):
    result = 1
    for index in range (0, mu):
        result = result * number
    return result

def input_list():

    length = int(input("Nhập vào độ dài của mảng: "))
    list[length]
    for index in range(0, length):
        test = input(f"Nhập vao phan tu thu {index} của mảng: ")
        list[index] = test
    print(list)

input_list()